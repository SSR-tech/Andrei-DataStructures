// Static arrays are fixed in size. Will be told ahead of time.
// Dynamic arrays allow us to copy a static array and rebuild an array in a new location if we want more memory.
// Dynamic arrays expand as we increase elements in it.